# .env.example - Copy to .env and fill in your values
# This file configures embedding models and other settings for the RAG app.
# Do NOT commit your .env file (it's ignored by .gitignore).

# HuggingFace Embedding Model (used for semantic search in FAISS)
# Default: all-MiniLM-L6-v2 (lightweight, fast for educational content)
HF_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Optional: Ollama Model Override (if you want to change from deepseek-r1:1.5b)
# Uncomment and set if needed (requires code edit in main_rag_app.py to load it)
# OLLAMA_MODEL=deepseek-r1:1.5b
